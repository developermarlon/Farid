import { connect } from 'react-redux';
import Component from 'screens/core/Main';


const mapStateToProps = state => {
    return {}
};

const mapDispatchToProps = dispatch => {
    return {};
}

export default connect(mapStateToProps, mapDispatchToProps)(Component);